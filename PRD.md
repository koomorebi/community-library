# 社区图书馆借阅管理系统

> PRD版本：v2.0（审查修复版）
> 最后更新：2026-06-26
> 修复来源：三方审查（数据架构师A- / 系统架构师B+ / 产品经理UX B）

---

## 产品愿景

**一句话**：让社区图书馆的借阅管理从"阿姨的纸质小本本"变成"扫一下就搞定"的数字化系统。

**不用它会怎样**：
- 书借出去了不知道在谁手里 → 资产流失
- 忘记还书无人提醒 → 流通率低
- 想查历史全靠翻本子 → 效率低下
- 不知道哪些书最受欢迎 → 采购无依据

---

## 用户画像

### 管理员（阿姨）

| 维度 | 描述 |
|------|------|
| 身份 | 社区图书馆管理员，日常负责借还登记 |
| 年龄 | 45-60岁，不太会用电脑 |
| 使用场景 | 会员来借/还书时操作，每天约20-50次 |
| 技术背景 | 会用微信、会打字，但不熟悉复杂软件 |
| 核心诉求 | **简单、不出错、能提醒** |
| 设备 | 社区配的台式电脑或平板 |
| 特殊需求 | 字体要大（≥16px）、按钮要大（≥44px）、颜色区分状态、操作有声音反馈 |

### 会员（社区居民）

| 维度 | 描述 |
|------|------|
| 身份 | 社区居民，各年龄段都有 |
| 使用场景 | 查书、借书、还书、查看自己的借阅记录 |
| 技术背景 | 差异大，从老人到年轻人都有 |
| 核心诉求 | **快速找到想看的书、知道什么时候该还** |
| 设备 | 手机为主 |

---

## 核心问题

| # | 问题 | 具体表现 | 映射功能 |
|---|------|---------|---------|
| 1 | 书在谁手里不知道 | 借出后无记录，书可能丢失 | 借还登记 + 库存追踪 |
| 2 | 忘记还书无提醒 | 书长期滞留在会员手中 | 逾期提醒 + 到期通知 |
| 3 | 想查历史查不到 | 不知道谁借过、借了多久 | 借阅记录查询 |
| 4 | 不了解流通情况 | 不知道哪些书热门、哪些无人问津 | 统计看板 |

---

## 功能列表

### MVP功能（第一期）

- [ ] **图书管理**：增删改查、ISBN自动填充、封面图、书架位置、状态管理（在馆/借出/维修）
- [ ] **会员管理**：注册、信息维护、借阅卡号生成、身份证号记录
- [ ] **借书流程**（2步简化版）：扫借阅卡号 → 扫图书编号 → 预览确认 → 借出
- [ ] **还书流程**：输入借阅卡号 → 显示待还列表 → 一键全部归还/选择归还
- [ ] **续借功能**：支持续借1次，延长30天
- [ ] **逾期检测**：自动标记逾期未还的借阅记录
- [ ] **快速搜索**：按书名/作者/ISBN模糊搜索，结果高亮匹配词
- [ ] **基础统计**：当前借出数、逾期数、馆藏总数、今日借还数
- [ ] **管理员登录**：JWT认证，防止未授权访问
- [ ] **操作撤销**：5分钟内可撤销误操作

### 后续迭代（v1.1+）

- 逾期提醒推送（短信/微信通知）
- 数据可视化看板（ECharts图表）
- 报表导出（PDF/Excel）
- 扫码借还（手机扫ISBN）
- 会员自助查询（手机端查看借阅记录）
- 智能推荐（根据借阅历史推荐）
- 预约功能
- 批量导入图书

---

## 用户流程

### 借书流程（v2.0 简化版 — 2步操作）

```
[管理员登录系统]
       │
       ▼
[首页大按钮：📖 借书] ──→ [进入借书页面]
       │
       ▼
[Step 1: 输入借阅卡号（大字体输入框）]
       │
       ├─→ [卡号不存在] ──→ [大红提示：❌ 未找到此卡号，请检查后重试]
       │
       └─→ [卡号有效] ──→ [显示会员信息卡片：姓名、当前借阅数、是否有逾期]
       │
       ├─→ [有逾期] ──→ [大黄提示：⚠️ 该会员有逾期图书，请先归还]
       │
       └─→ [无逾期，继续]
       │
       ▼
[Step 2: 输入图书编号（大字体输入框，支持连续输入多本）]
       │
       ├─→ [图书不存在] ──→ [大红提示：❌ 未找到此书]
       │
       ├─→ [已借出] ──→ [大红提示：❌ 此书已被借出]
       │
       └─→ [在馆] ──→ [加入待借列表，绿色标识 ✅]
       │
       ▼
[预览确认页面]
  ┌────────────────────────────────────┐
  │  📖 即将借出以下图书：              │
  │                                    │
  │  ✅ 《三体》- 应还日期：2026-07-26  │
  │  ✅ 《活着》- 应还日期：2026-07-26  │
  │                                    │
  │  借阅人：张阿姨（卡号：LIB-001）    │
  │                                    │
  │  [取消]         [✅ 确认借出]       │
  └────────────────────────────────────┘
       │
       ▼
[确认借出] ──→ [大绿提示：✅ 借书成功！（+ 声音提示）]
       │
       ▼
[显示操作成功 + 5分钟内可撤销]
```

### 还书流程（v2.0 简化版）

```
[管理员登录系统]
       │
       ▼
[首页大按钮：↩️ 还书] ──→ [进入还书页面]
       │
       ▼
[输入借阅卡号或图书编号]
       │
       ├─→ [按卡号查询] ──→ [显示该会员所有待还图书列表]
       │     │
       │     ▼
       │   [一键全部归还] 或 [勾选部分归还]
       │     │
       │     ▼
       │   [预览确认：显示是否逾期 + 逾期天数]
       │     │
       │     ▼
       │   [确认归还] ──→ [大绿提示：✅ 归还成功！]
       │
       └─→ [按图书编号查询] ──→ [显示该书借阅信息]
             │
             ▼
           [确认归还] ──→ [大绿提示：✅ 归还成功！]
```

### 逾期处理流程

```
[系统定时扫描（每天凌晨2点）]
       │
       ▼
[筛选 due_date < 今天 且 return_date IS NULL]
       │
       ▼
[更新Borrow表：标记逾期状态]
       │
       ▼
[生成逾期报告] ──→ [管理员首页显示逾期提醒卡片（红色醒目）]
```

---

## 数据结构

### ER关系图

```
Admin (管理员)
  │
  └── operator_id ──→ Borrow (借阅记录)

Book (图书信息) 1:N BookCopy (馆藏副本)
  │                    │
  │                    └── copy_id ──→ Borrow (借阅记录)
  │
  └── category_id ──→ Category (分类)

Member (会员)
  │
  └── member_id ──→ Borrow (借阅记录)
```

### Admin（管理员）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password_hash | VARCHAR(128) | 密码哈希（bcrypt） |
| name | VARCHAR(50) | 显示名称 |
| is_active | BOOLEAN | 是否启用，默认True |
| created_at | DATETIME | 创建时间 |

### Book（图书信息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| title | VARCHAR(200) | 书名 |
| author | VARCHAR(100) | 作者 |
| isbn | VARCHAR(25) | ISBN编号，唯一（兼容ISBN-10和ISBN-13） |
| publisher | VARCHAR(100) | 出版社 |
| category_id | INTEGER | 外键，关联Category表 |
| cover_image | VARCHAR(500) | 封面图URL/路径 |
| description | TEXT | 图书简介 |
| total_copies | INTEGER | 总册数（冗余字段，= BookCopy记录数） |
| available_copies | INTEGER | 可借册数（冗余字段，借还时事务更新） |
| created_at | DATETIME | 入库时间 |
| updated_at | DATETIME | 最后更新时间 |
| is_deleted | BOOLEAN | 软删除标记，默认False |

**索引**：isbn（唯一）、title（全文索引）、author

### BookCopy（馆藏副本） — 解决"一本书多副本"问题

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| book_id | INTEGER | 外键，关联Book |
| copy_no | VARCHAR(20) | 副本编号（如：LIB-001-C01） |
| book_location | VARCHAR(50) | 书架位置（如：A区-3排-2层） |
| status | ENUM | 状态：available / borrowed / maintenance |
| created_at | DATETIME | 入库时间 |

**索引**：book_id、status

**说明**：Book存"书的信息"，BookCopy存"书的实体"。同一种书3本 = 1条Book + 3条BookCopy。

### Category（图书分类）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | VARCHAR(50) | 分类名称，唯一（如：文学、科技、历史） |
| sort_order | INTEGER | 排序权重 |

### Member（会员）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | VARCHAR(50) | 姓名 |
| phone | VARCHAR(25) | 手机号，唯一（兼容国际号码） |
| id_card | VARCHAR(20) | 身份证号（可选，实名制场景） |
| card_no | VARCHAR(20) | 借阅卡号，自动生成，唯一 |
| address | VARCHAR(200) | 地址（可选，送书上门场景） |
| join_date | DATE | 注册日期 |
| status | ENUM | 状态：active / frozen |
| max_borrows | INTEGER | 最大借阅数，默认5 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后更新时间 |
| is_deleted | BOOLEAN | 软删除标记，默认False |

**索引**：card_no（唯一）、phone（唯一）、name

### Borrow（借阅记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| book_copy_id | INTEGER | 外键，关联BookCopy（具体副本） |
| member_id | INTEGER | 外键，关联Member |
| operator_id | INTEGER | 外键，关联Admin（谁执行的操作） |
| borrow_date | DATETIME | 借出时间 |
| due_date | DATE | 应还日期（默认借出后30天，可配置） |
| return_date | DATETIME | 实际归还时间，NULL表示未还 |
| renew_count | INTEGER | 续借次数，默认0，最大1 |
| status | ENUM | 状态：borrowed / returned / overdue |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后更新时间 |

**索引**：book_copy_id、member_id、due_date、borrow_date、status

**说明**：
- 逾期状态（overdue）由定时任务每天扫描更新：`due_date < 今天 AND return_date IS NULL`
- 续借逻辑：`renew_count < 1` 时可续借，`due_date += 30天`，`renew_count += 1`

### 数据关系

```
Admin (1) ──── (N) Borrow (N) ──── (1) Member
                        │
Book (1) ──── (N) BookCopy (1) ────┘
  │
  └── category_id ──→ Category
```

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (Vue 3)                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 图书管理  │  │ 会员管理  │  │ 借还操作  │  │ 统计看板  │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│       └──────────────┴──────────────┴──────────────┘         │
│                            │ API (/api/v1/)                  │
└────────────────────────────┼────────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────┐
│                     后端 (FastAPI)                           │
│  ┌─────────────────────────┼─────────────────────────┐      │
│  │         Middleware 层 (中间件)                     │      │
│  │  CORS │ JWT认证 │ 请求日志 │ 异常处理 │ 输入校验   │      │
│  └─────────────────────────┼─────────────────────────┘      │
│  ┌─────────────────────────┼─────────────────────────┐      │
│  │              Router 层 (路由)                      │      │
│  │  /api/v1/books  /members  /borrows  /stats        │      │
│  └─────────────────────────┼─────────────────────────┘      │
│  ┌─────────────────────────┼─────────────────────────┐      │
│  │              Service 层 (业务逻辑)                 │      │
│  │  借书校验  还书处理  逾期检测  统计计算             │      │
│  └─────────────────────────┼─────────────────────────┘      │
│  ┌─────────────────────────┼─────────────────────────┐      │
│  │              Model 层 (数据访问)                   │      │
│  │  SQLAlchemy ORM                                   │      │
│  └─────────────────────────┼─────────────────────────┘      │
└────────────────────────────┼────────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────┐
│                     数据库 (SQLite/MySQL)                    │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │admins│ │books │ │book_ │ │cate- │ │mem-  │ │borrows│   │
│  │      │ │      │ │copies│ │gories│ │bers  │ │      │    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## API设计规范

### 版本管理

所有API使用 `/api/v1/` 前缀，便于后续升级。

### 统一响应格式

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

**错误响应**：
```json
{
  "code": 400,
  "message": "该会员有逾期图书，请先归还",
  "detail": { "overdue_books": ["三体", "活着"] }
}
```

**标准错误码**：

| 错误码 | 含义 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证（未登录） |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 业务冲突（如：库存不足） |
| 500 | 服务器内部错误 |

### 认证机制

- 使用 JWT（JSON Web Token）认证
- 登录接口：`POST /api/v1/auth/login` → 返回 access_token
- 所有其他接口需在 Header 中携带：`Authorization: Bearer <token>`
- Token有效期：24小时

---

## 权限设计

| 角色 | 权限 |
|------|------|
| 管理员 | 全部功能：图书管理、会员管理、借还操作、统计查看 |
| 会员（预留） | 仅查看：搜索图书、查看自己的借阅记录 |

> MVP阶段暂不实现会员端，仅管理员端。

---

## 事务与并发设计

### 借书事务边界

```
BEGIN TRANSACTION
  1. 检查会员状态（active、无逾期、未达上限）
  2. 检查BookCopy状态（available）
  3. 更新BookCopy.status → borrowed
  4. 更新Book.available_copies -= 1
  5. 插入Borrow记录
COMMIT
```

**任何一步失败，全部回滚。**

### 还书事务边界

```
BEGIN TRANSACTION
  1. 查找Borrow记录（book_copy_id + status=borrowed）
  2. 更新Borrow.return_date、status=returned
  3. 更新BookCopy.status → available
  4. 更新Book.available_copies += 1
COMMIT
```

### 并发控制

- **乐观锁**：BookCopy表增加 `version` 字段，更新时检查版本号
- 冲突时返回409错误：`"此书刚被其他人借出，请刷新后重试"`
- SQLite开发阶段使用WAL模式提升并发读写性能

### 逾期状态处理

- Borrow.status 中的 `overdue` 为**计算+存储**混合模式
- 定时任务每天凌晨2点扫描：`UPDATE borrow SET status='overdue' WHERE due_date < DATE('now') AND return_date IS NULL AND status = 'borrowed'`
- 补偿策略：全量扫描（数据量小，社区图书馆通常<1万条记录）
- 定时任务失败时记录日志，下次执行自动补扫

---

## UX设计规范

### 视觉规范

| 元素 | 规范 |
|------|------|
| 基础字号 | ≥16px（正文）、≥20px（标题）、≥28px（大按钮文字） |
| 按钮最小尺寸 | 44px × 44px（触控友好） |
| 颜色状态 | 🟢 绿色 = 在馆/成功、🔴 红色 = 已借出/逾期/错误、🟡 黄色 = 维修/警告、⚪ 灰色 = 归档 |
| 莫兰迪配色 | 背景 #F2EDE8、点缀 #C4A8A4、辅助 #AFC0AB |
| 间距 | 卡片间距 ≥16px，按钮间距 ≥12px |

### 交互规范

| 场景 | 规范 |
|------|------|
| 操作成功 | 大绿色提示框 + ✅ 图标 + 声音提示（可选） |
| 操作失败 | 大红色提示框 + ❌ 图标 + 友好文案（非技术语言） |
| 危险操作 | 二次确认弹窗（删除、批量操作） |
| 误操作撤销 | 5分钟内可撤销（显示撤销按钮倒计时） |
| 加载状态 | 骨架屏或加载动画 |
| 空状态 | 引导文案（如：暂无图书，点击"新增图书"开始） |

### 错误提示文案规范

| 技术错误 | 用户友好文案 |
|----------|-------------|
| "已达最大借阅数" | "您已经借了5本书，先还一些再借新的吧 📚" |
| "该会员有逾期" | "这位会员还有书没还哦，请先归还再借新书 ⚠️" |
| "库存不足" | "这本书暂时都被借走了，过几天再来看看 📖" |
| "记录不存在" | "没有找到这条记录，请检查输入是否正确 🔍" |

---

## 边界场景

### 用户行为异常

| 场景 | 应对方案 |
|------|---------|
| 借书时会员有逾期未还 | 阻止借书，大黄提示"请先归还逾期图书" |
| 同一本书被多人同时借 | 乐观锁控制，后请求者提示"已被借出，请刷新" |
| 借书后立刻想还 | 允许，无时间限制 |
| 输入不存在的会员/图书 | 友好提示"未找到"，引导重新输入 |
| 会员借阅数已达上限 | 阻止借书，提示"您已经借了5本书，先还一些再借新的吧" |
| 借错书 | 5分钟内可撤销操作 |
| 续借后还想续借 | 提示"每本书只能续借1次哦" |

### 数据异常

| 场景 | 应对方案 |
|------|---------|
| 图书库存数据不一致 | 借还操作使用事务 + 乐观锁，保证原子性 |
| 重复录入同一本书 | ISBN唯一约束，提示"已存在" |
| 删除有借阅记录的图书 | 软删除（is_deleted=True），保留历史 |
| 删除有借阅记录的会员 | 软删除（is_deleted=True），保留历史 |
| available_copies与实际不符 | 定时任务定期校验，发现不一致自动修复并记录日志 |

### 系统异常

| 场景 | 应对方案 |
|------|---------|
| 网络中断/数据库不可用 | 前端提示"系统繁忙，请稍后重试"，后端记录日志 |
| 并发借同一本书 | 乐观锁（version字段），冲突者提示"已被借出" |
| 逾期检测定时任务失败 | 记录失败日志，下次执行全量补扫 |
| JWT token过期 | 前端自动跳转登录页 |

---

## MVP范围

### 第一期包含

- [x] 管理员登录（JWT认证）
- [x] 图书CRUD（含封面图、书架位置、分类）
- [x] 会员CRUD（含身份证号、地址）
- [x] 借书流程（2步简化版 + 预览确认 + 撤销）
- [x] 还书流程（一键全部归还 + 逾期预览）
- [x] 续借功能
- [x] 逾期状态自动标记
- [x] 基础搜索（书名/作者/ISBN，结果高亮）
- [x] 首页统计卡片（总馆藏、借出中、逾期数、今日借还）
- [x] 统一错误响应 + 友好提示文案
- [x] 大字体大按钮UX适配

### 第一期不包含

- [ ] 逾期提醒推送（短信/微信）
- [ ] 数据可视化图表（ECharts）
- [ ] 报表导出（PDF/Excel）
- [ ] 扫码功能
- [ ] 会员自助端
- [ ] 多分馆支持
- [ ] 预约功能
- [ ] 批量导入

---

## 后续迭代路线

| 版本 | 内容 | 预计周期 |
|------|------|---------|
| v1.0 | MVP核心功能 | 5-6天 |
| v1.1 | 数据看板 + 报表导出 | 2天 |
| v1.2 | 逾期提醒推送 | 1天 |
| v2.0 | 会员自助端（手机H5） | 3天 |
| v2.1 | 扫码借还 | 2天 |
| v2.2 | 预约功能 + 批量导入 | 2天 |

---

## 技术栈

| 层 | 技术选型 | 理由 |
|----|---------|------|
| 前端 | Vue 3 + Element Plus | 组件成熟、中文友好、大字体适配 |
| 后端 | Python + FastAPI | 开发快、自动API文档、面试认可度高 |
| ORM | SQLAlchemy | 数据库抽象，切换成本低 |
| 认证 | PyJWT + bcrypt | JWT无状态认证，bcrypt密码哈希 |
| 数据库 | SQLite（开发，WAL模式）→ MySQL（生产） | 零配置启动，生产可迁移 |
| 构建工具 | Vite | 快速热更新 |

---

## 开发任务拆解

### Phase 1：基础搭建（Day 1）

- [ ] 初始化后端项目（FastAPI + SQLAlchemy）
- [ ] 设计并创建数据库表（Admin/Book/BookCopy/Category/Member/Borrow）
- [ ] 实现JWT认证中间件
- [ ] 实现统一错误响应格式
- [ ] 初始化前端项目（Vue 3 + Element Plus）
- [ ] 配置前后端联调（CORS / API代理）
- [ ] 实现管理员登录页

### Phase 2：图书管理（Day 2）

- [ ] 图书列表页（分页、搜索、颜色状态标识）
- [ ] 图书新增/编辑表单（含封面图上传、分类选择、书架位置）
- [ ] 图书详情页（封面图、简介、当前借阅人、副本列表）
- [ ] 图书删除（软删除 + 二次确认）
- [ ] 分类管理（CRUD）

### Phase 3：会员管理（Day 3）

- [ ] 会员列表页（大字体、颜色状态）
- [ ] 会员注册/编辑表单（含身份证号、地址）
- [ ] 借阅卡号自动生成逻辑
- [ ] 会员状态管理（正常/冻结）
- [ ] 会员详情页（借阅历史、当前借阅、逾期记录）

### Phase 4：借还流程（Day 4-5）

- [ ] 借书页面（2步简化版：扫卡→扫书→预览→确认）
- [ ] 借书校验逻辑（库存、逾期、上限、乐观锁）
- [ ] 借书撤销功能（5分钟窗口）
- [ ] 还书页面（一键全部归还 + 逾期预览）
- [ ] 续借功能
- [ ] 逾期状态自动标记（定时任务）
- [ ] 借阅记录查询

### Phase 5：统计与优化（Day 6）

- [ ] 首页统计卡片（总馆藏、借出中、逾期数、今日借还）
- [ ] 借阅历史查询
- [ ] UI优化（莫兰迪配色、大字体、大按钮、引导提示、声音反馈）
- [ ] 边界场景测试
- [ ] README文档编写

---

## 面试展示要点

| 能力 | 展示方式 |
|------|---------|
| 产品思维 | PRD文档、用户流程、边界场景、三方审查修复 |
| 数据库设计 | ER图、Book/BookCopy分离设计、索引优化、事务处理 |
| 前端能力 | Vue组件化、Element Plus使用、响应式布局、UX适配 |
| 后端能力 | FastAPI路由、JWT认证、ORM操作、API版本管理 |
| 业务逻辑 | 借还校验、逾期检测、乐观锁并发控制、撤销机制 |
| 文档能力 | README、API文档（Swagger）、代码注释 |

---

## 参考资源

**GitHub开源项目**：
- `myide/django-vue.js-book` ⭐307 — Django + Vue 前后端分离
- `vinitshahdeo/Library-Management-System` ⭐261 — Web全栈
- `iampranavdhar/Library-Management-System-MERN` ⭐165 — MERN栈

**专业ILS（参考但不照搬）**：
- Koha — 全球最成熟开源ILS
- Evergreen — 多分馆场景

---

## 审查修复记录

| 来源 | 修复项 | 状态 |
|------|--------|------|
| 数据架构师 | Book表增加cover_image、book_location、description字段 | ✅ |
| 数据架构师 | 新增BookCopy表解决多副本问题 | ✅ |
| 数据架构师 | Borrow表增加renew_count、operator_id字段 | ✅ |
| 数据架构师 | 新增Category独立表（替代VARCHAR分类） | ✅ |
| 数据架构师 | isbn VARCHAR(20)→(25)、phone VARCHAR(20)→(25) | ✅ |
| 数据架构师 | 增加索引设计（Borrow全字段、Book title/author） | ✅ |
| 数据架构师 | Book.status与available_copies冲突 → 移除Book.status，状态下沉到BookCopy | ✅ |
| 数据架构师 | 增加软删除字段（is_deleted） | ✅ |
| 系统架构师 | 新增Admin表 + JWT认证机制 | ✅ |
| 系统架构师 | API增加 /api/v1/ 前缀 | ✅ |
| 系统架构师 | 统一错误响应格式 + 错误码 | ✅ |
| 系统架构师 | 明确借书/还书事务边界 | ✅ |
| 系统架构师 | 乐观锁设计（BookCopy.version字段） | ✅ |
| 系统架构师 | 逾期状态处理策略（计算+存储混合） | ✅ |
| 系统架构师 | 中间件层设计（CORS/JWT/日志/异常处理） | ✅ |
| UX产品经理 | 借书流程从4步简化为2步 | ✅ |
| UX产品经理 | 增加预览确认页面（借出前/归还前） | ✅ |
| UX产品经理 | 增加一键全部归还功能 | ✅ |
| UX产品经理 | 增加5分钟撤销机制 | ✅ |
| UX产品经理 | 大字体(≥16px)、大按钮(≥44px)、颜色状态 | ✅ |
| UX产品经理 | 友好错误提示文案（非技术语言） | ✅ |
| UX产品经理 | 操作成功/失败反馈（大提示框+图标） | ✅ |
| UX产品经理 | 会员详情页增加借阅历史 | ✅ |
| UX产品经理 | 图书详情页增加封面图、简介、当前借阅人 | ✅ |
